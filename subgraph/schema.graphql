type Company @entity {
  id: ID!
  walletAddress: Bytes!
  name: String!
  profilePhoto: String
  tokenName: String
  description: String
  includeCount: Int!
  includedByCount: Int!
}

type Eco @entity {
  id: ID!
  company1Address: Bytes!
  company2Address: Bytes!
  isIncluded: Boolean!
}
